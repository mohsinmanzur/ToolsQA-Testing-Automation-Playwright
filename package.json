{
  "name": "demoqa-playwright-framework",
  "version": "1.0.0",
  "description": "Playwright + JS automation framework for demoqa.com",
  "scripts": {
    "test": "npx playwright test",
    "test:report": "npx playwright test && npx allure generate allure-results --clean -o reports/allure-report",
    "report:open": "npx allure open reports/allure-report",
    "test:debug": "npx playwright test --debug",
    "test:login": "npx playwright test tests/login.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "allure-playwright": "^3.10.0",
    "allure-commandline": "^2.42.1",
    "winston": "^3.19.0"
  }
}
